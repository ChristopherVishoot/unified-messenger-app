apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.cache.name }}-service"
  namespace: "{{ .Values.namespace }}"
  labels:
    app: "{{ .Values.cache.name }}"
spec:
  ports:
  - port: {{ .Values.cache.port }}
    name: cache
  clusterIP: None
  selector:
    app: "{{ .Values.cache.name }}"