apiVersion: v1
kind: Service
metadata:
  name: cache-service
  namespace: hello
  labels:
    app: cache-service
spec:
  ports:
  - port: 6379
    name: cache
  clusterIP: None
  selector:
    app: cache